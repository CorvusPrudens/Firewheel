[package]
name = "stream_nodes"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
firewheel = { path = "../../", features = ["stream_nodes"] }
log.workspace = true
simple-log = "2.1.0"